package Learn;
import java.util.Scanner;

public class stringMethod {
    public static void main(String[] args) {
        String name = "Khacmanh";
        // boolean result = name.equals("Khacmanh"); // chek if those equals?
        // int result = name.length(); // return the length if that string
        // char result = name.charAt(0); // return char at name[n];
        // int result = name.indexOf("c") // return the index of "c" in the string
        // boolean result = name.isEmpty(); // chek if empty
        // String result = name.toUpperCase(); // set result as name's all uppercase
        // String result = name.toLowerCase(); // set result as name's all lowercase
        // String result = name.trim(); // remove all the spaces in the string name
        // String result = name.replace('c', 'z'); // replace c to z

        // System.out.println(result);
    }
}
